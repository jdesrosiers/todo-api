{
    "name": "hypermedia-todo",
    "scripts": {
        "test": "vendor/bin/phpunit"
    },
    "env": {
        "MONGODB_DBNAME": {
            "required": true
        },
        "MONGODB_URI": {
            "required": true
        }
    },
    "formation": {
        "web": {
            "quantity": 1
        }
    },
    "addons": [
        "mongolab"
    ],
    "buildpacks": [
        {
            "url": "heroku/php"
        }
    ]
}
